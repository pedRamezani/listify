{
  "name": "listify",
  "private": true,
  "version": "1.0.0",
  "description": "Website to convert text into arrays/lists for programming.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "codemirror": "^6.0.1"
  }
}
